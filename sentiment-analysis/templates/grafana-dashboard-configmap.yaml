apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sentiment-chart.fullname" . }}-grafana-dashboard
  labels:
    grafana_dashboard: "1"
data:
  sentiment-experiment-dashboard.json: |-
{{ .Files.Get "files/grafana-dashboard.json" | indent 4 }}