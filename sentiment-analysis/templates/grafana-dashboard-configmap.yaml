apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sentiment-chart.fullname" . }}-dashboards
  labels:
    grafana_dashboard: "1"
data:
  sentiment-dashboard.json: |
   {{ .Files.Get "dashboards/sentiment-dashboard.json" | indent 4 }}