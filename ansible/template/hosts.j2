127.0.0.1 localhost

{% for host in groups['ctrl'] %}
{{ hostvars[host]['ansible_host'] | default(host) }} ctrl
{% endfor %}

{% for host in groups['workers'] %}
{{ hostvars[host]['ansible_host'] | default(host) }} {{ hostvars[host]['inventory_hostname'] }}
{% endfor %}